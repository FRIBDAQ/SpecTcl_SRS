/*
    This software is Copyright by the Board of Trustees of Michigan
    State University (c) Copyright 2014.

    You may use this software under the terms of the GNU public license
    (GPL).  The terms of this license are described at:

     http://www.gnu.org/licenses/gpl.txt

    Author:
             Simon Giraud
         FRIB
         Michigan State University
         East Lansing, MI 48824-1321
*/
#include <config.h>
#include "CSRS.h"
#include <EventProcessor.h>
#include <TCLAnalyzer.h>
#include <Event.h>
#include <math.h>
#include <Spectrum.h>
#include <Histogrammer.h>
#include <Globals.h>
#include <netinet/in.h>
#include <iostream>
#include <memory>

#include "CSRSUnpacker.h"
#include "Configuration.h"

#ifdef HAVE_STDNAMESPACE
using namespace std;
#endif

//---------------------------------------------------------//
// class CSRS
CSRS::CSRS(string name)
{
    m_name = name;
}

CSRS::~CSRS()
{
    delete[] dataRaw;
    dataRaw = nullptr;
    delete[] dataCal;
    dataCal = nullptr;
}

void CSRS::Initialize()
{
    runtime.Initialize(m_name + ".runtime", "sec");
    eventlength.Initialize(m_name + ".eventlength", 8193, 0, 8192, "bytes");
    nHits.Initialize(m_name + ".nHits", 2000, 0, 2000, "");
    debugMode.Initialize(m_name + ".debugMode", 0, "debug");

    for (int i = 0; i < MaxFECs; i++)
    {
        string fecIdStr = to_string(i);
        fec[i].Initialize(this, m_name + ".fec" + fecIdStr, i);
    }
}

void CSRS::Reset()
{
    runtime.Reset();
    eventlength.Reset();
    nHits.Reset();


    for (int i = 0; i < MaxFECs; i++)
    {
        for (int j = 0; j < MaxVMMs; j++)
        {
            if (config.pFecVmm[i][j])
            {
                fec[i].Reset();
                break;
            }
        }
        nbEvents[i] = 0;
    }
}

//---------------------------------------------------------//
// class CSRSFec
CSRSFec::~CSRSFec()
{
    delete top;
    top = nullptr;
}

void CSRSFec::Initialize(CSRS *theTop, string name, int fecId)
{
    top = theTop;


    
    for (int i = 0; i < MaxVMMs; i++)
    {
        if (config.pFecVmm[fecId][i])
        {
            evt.Initialize(name + ".evt", "");
            nHits.Initialize(name + ".nHits", "");
        }
        string vmmIdStr = to_string(i);
        vmm[i].Initialize(top, name + ".vmm" + vmmIdStr, fecId, i);
    }

    overThreshold = false;
    // if (top->debugMode)
    // {
    //     cou<<"debug..."<<endl;
    // }
}

void CSRSFec::Reset()
{
    for (int i = 0; i < MaxVMMs; i++)
    {
        vmm[i].Reset();
    }
    nHits.Reset();
    evt.Reset();
}

//---------------------------------------------------------//
// class CSRSVmm
CSRSVmm::~CSRSVmm()
{
    delete top;
    top = nullptr;
}

void CSRSVmm::Initialize(CSRS *theTop, string name, int fecId, int vmmId)
{
    top = theTop;
    if (config.pFecVmm[fecId][vmmId])
    {
        cout << "Name - " << name << endl;
        adc.Initialize(name + ".adc", 1024, 0.0, 1023.0, "channels", 64, 0);
        adcCal.Initialize(name + ".adcCal", 1024, 0.0, 1023.0, "channels", 64, 0);
        tdc.Initialize(name + ".tdc", 256, 0.0, 255.0, "channels", 64, 0);
        correctedTime.Initialize(name + ".correctedTime", 10000, 0.0, 1000000.0, "a.u.", 64, 0);
        // timeStamp.Initialize(name + ".timeStamp", 3e+9, 5e+9, 100000, "ticks", 64, 0);
        timeStamp.Initialize(name + ".timeStamp", "ticks");
        timeStampDiff.Initialize(name + ".timeStampDiff", "");
    }
    // if (top->debugMode)
    // {
    //     cou<<"debug..."<<endl;
    // }
}

void CSRSVmm::Reset()
{
    adc.Reset();
    adcCal.Reset();
    tdc.Reset();
    correctedTime.Reset();
    hasdata = kfFALSE;
}

void CSRS::SetDataRaw(Gem::ParserSRS::VMM3Data *dataArray)
{
    // std::vector<double> v_ts_fec6;
    // std::vector<double> v_ts_fec7;
    // double buff_ts_fec6 = -1;
    // double buff_ts_fec7 = -1;
    double ts_prev[MaxFECs][MaxVMMs]={0};
    double ts_sum[MaxFECs][MaxVMMs]={0};
    int countSum[MaxFECs][MaxVMMs]={0};
    for (int i = 0; i < srs.nHits; i++)
    {
        auto &d = dataArray[i];
        int fecId = dataParser.fecId;
        int vmmId = d.vmmid;
        int chNo = d.chno;

        printf("SRS Data: fec: %d, vmm: %d, channel: %d, overThreshold: %d, adc: %d, tdc: %d, fecTimeStamp: %d, triggerOffset: %d  \n",dataParser.fecId,d.vmmid, d.chno, d.overThreshold, d.adc, d.tdc,d.fecTimeStamp,d.triggerOffset);
        if (config.pFecVmm[fecId][vmmId])
        {
            nbEvents[fecId] += 1;
            //there will be one TS per fec.vmm but in the data it can be several.... pick the last one...
            srs.fec[fecId].vmm[vmmId].timeStamp = d.fecTimeStamp;
             
            //if (timeStampPrev[fecId][vmmId])
            if (ts_prev[fecId][vmmId]>1)
            {
                ts_sum[fecId][vmmId] += srs.fec[fecId].vmm[vmmId].timeStamp - ts_prev[fecId][vmmId];
                countSum[fecId][vmmId] += 1;
                std::cout<<"TS, vmm"<<vmmId<<" ihit "<<i<<" : "<<srs.fec[fecId].vmm[vmmId].timeStamp<<" "<<ts_prev[fecId][vmmId]<<" "<<ts_sum[fecId][vmmId]<<" "<<srs.fec[fecId].vmm[vmmId].timeStamp - ts_prev[fecId][vmmId]<<" "<<ts_sum[fecId][vmmId]/countSum[fecId][vmmId]<<std::endl;
            }
            ts_prev[fecId][vmmId] = srs.fec[fecId].vmm[vmmId].timeStamp;
            //std::cout<<"TS, vmm"<<vmmId<<" ihit "<<i<<" : "<<srs.fec[fecId].vmm[vmmId].timeStamp<<" "<<srs.fec[fecId].vmm[vmmId].timeStampDiff<<std::endl;

            
            // if (fecId == 6 && buff_ts_fec6 != d.fecTimeStamp)
            // {
            //     v_ts_fec6.push_back(d.fecTimeStamp);
            //     buff_ts_fec6 = d.fecTimeStamp;
            //     srs.fec[fecId].vmm[vmmId].timeStamp = buff_ts_fec6;
            //     std::cout<<"ts fec6, vmm"<<vmmId<<" ihit"<<i<<" : "<<buff_ts_fec6<<" "<<srs.fec[fecId].vmm[vmmId].timeStamp<<std::endl;
            // }
            // else if (fecId == 7 && buff_ts_fec7 != d.fecTimeStamp)
            // {
            //     v_ts_fec7.push_back(d.fecTimeStamp);
            //     buff_ts_fec7 = d.fecTimeStamp;
            //     // srs.fec[fecId].vmm[vmmId].timeStamp = d.fecTimeStamp;
            //     srs.fec[fecId].vmm[vmmId].timeStamp = buff_ts_fec7;
            //     std::cout<<"ts fe7, vmm"<<vmmId<<" ihit"<<i<<" : "<<buff_ts_fec7<<std::endl;
            // }
            // if (nbEvents[fecId] > 0) srs.fec[fecId].timeStampDiff = d.fecTimeStamp - srs.fec[fecId].timeStamp;
            // srs.fec[fecId].timeStamp = d.fecTimeStamp;
            // if (srs.nbEvents > 0) srs.fec[fecId].timeStampDiff = d.fecTimeStamp - fecTimeStampPrev[fecId];
            // srs.timeStamp[fecId] = d.fecTimeStamp;




            //commented for TS test, should have following lines if data
            //srs.fec[fecId].vmm[vmmId].adc[chNo] = d.adc;
            //srs.fec[fecId].vmm[vmmId].tdc[chNo] = d.tdc;
            //srs.fec[fecId].vmm[vmmId].hasdata = true;
        }
        else
        {
            printf("CSRS::SetDataRaw - fecId: %d or/and vmmId: %d not listed in the configuration file, the corresponding data will not be in the treegui \n", fecId, vmmId);
        }

    } // for hits

    

    dataRaw = dataArray;
    for (int id=0; id < MaxFECs; id++){
        cout << "CSRS::SetDataRaw - timeStampPrev"<<id<<" "<<nbEvents[id]<< endl;
        if (nbEvents[id]>0) {
            srs.fec[id].nHits = nbEvents[id];
            eventNumber[id] += 1;
            srs.fec[id].evt = eventNumber[id];
            for(int idvmm=0; idvmm < MaxVMMs; idvmm++){
                if (config.pFecVmm[id][idvmm])
                {
                    cout << "CSRS::SetDataRaw - timeStampPrev"<<idvmm<< endl;
                    try
                    {
                        srs.fec[id].vmm[idvmm].timeStampDiff = ts_sum[id][idvmm] / (countSum[id][idvmm]);
                        //srs.fec[id].vmm[idvmm].timeStampDiff = ts_sum[id][idvmm] / (nbEvents[id]);
                       // timeStampPrev[id][idvmm] = srs.fec[id].vmm[idvmm].timeStamp;
                        std::cout<<"TS, fec, vmm"<<id<<" "<<idvmm<<" "<<srs.fec[id].vmm[idvmm].timeStampDiff<<" "<<countSum[id][idvmm]<<" "<<nbEvents[id]<<std::endl;

                    }
                    catch (exception &exc)
                    {
                            cout << "CSRS::SetDataRaw - timeStampPrev" << exc.what() << endl;
                    }
                    // srs.fec[id].vmm[idvmm].timeStampDiff = srs.fec[id].vmm[idvmm].timeStamp - timeStampPrev[id][idvmm];
                    //std::cout<<"CSRS::timeStamp ---------------------------------- "<<id<<" "<<idvmm<<" "<<srs.fec[id].vmm[idvmm].timeStamp<<std::endl;
                    // timeStampPrev[id][idvmm] = srs.fec[id].vmm[idvmm].timeStamp;
                    // std::cout<<"CSRS::timeStamp ---------------------------------- "<<id<<" "<<idvmm<<" "<<timeStampPrev[id][idvmm]<< " "<<srs.fec[id].vmm[vmmId].timeStampDiff<<std::endl;

                }
            }
        }
    }

    // for (int i = 0; i < v_ts_fec6.size(); i++)
    // {
    //     std::cout<<"v_ts_fec6 "<<v_ts_fec6.at(i)<<std::endl;
    // }
    // for (int i = 0; i < v_ts_fec7.size(); i++)
    // {
    //     std::cout<<"v_ts_fec7 "<<v_ts_fec7.at(i)<<std::endl;
    // }

    
}

void CSRS::SetDataCal(Gem::ParserSRS::VMM3DataCal *dataCalArray)
{
    for (int i = 0; i < srs.nHits; i++)
    {
        auto &d = dataCalArray[i];
        int fecId = dataParser.fecId;
        int vmmId = d.vmmid;
        int chNo = d.chno;

        // printf("SRS Data Cal: fec: %d, vmm: %d, channel: %d, overThreshold: %d, adc cal.: %d, tdc: %d, timeStamp: %d, correctedTime: %d  \n",dataParser.fecId,d.vmmid, d.chno, d.overThreshold, d.adc, d.tdc,d.timeStamp,d.correctedTime);
        if (config.pFecVmm[fecId][vmmId])
        {
            srs.fec[fecId].vmm[vmmId].adcCal[chNo] = d.adc;
            srs.fec[fecId].vmm[vmmId].correctedTime[chNo] = d.correctedTime;
        }
        // else {
        // warning already printed for raw data
        // printf("CSRS::SetDataCal - fecId: %d or/and vmmId: %d not listed in the configuration file, the corresponding data will not be in the treegui \n",fecId,vmmId);
        // }

    } // for hits
    dataCal = dataCalArray;
}

